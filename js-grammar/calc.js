let before = 1000000;
let after = before * (1.06 ** 10);
let answer = after - before;
// document.write(answer); // 答えを出力

// 円の面積（練習問題）
let hankei = 12;
let menseki = (hankei ** 2) * 3.14;
document.write(menseki);
