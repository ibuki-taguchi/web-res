const age = 20;
const member = false;

console.log(1 + age);

if (age <= 15) {
  document.write("チケットの値段は800円");
} else if (member) {
  document.write("チケットの値段は1000円");
} else {
  document.write("チケットの値段は1800円");
}
